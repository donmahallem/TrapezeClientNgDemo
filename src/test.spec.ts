/*!
 * Source https://github.com/donmahallem/TrapezeApiTypes
 */

import { expect } from "chai";
import "mocha";

describe("start", () => {
    it("should pass", () => {
        expect(2).to.lessThan(4);
    });
});
